public enum GenerationMode {

    // for terms
    A,

    // for goals
    G,

    // optimized code generation for unification
    U,

    // for clauses
    C,

    // for predicates (a disjunction of clauses)
    P
}
